<div class="row">
  <div class="col-md-10 offset-md-1">
    <h2>Mestá</h2>
    <table class="table table-bordered">
      <thead>
        <tr>
          <th scope="col" class="align-middle">#</th>
          <th scope="col" class="align-middle">Názov</th>
          <th scope="col" class="align-middle">PSČ</th>
          <th scope="col" class="align-middle">Kód regiónu</th>
          <th>
            <a href="/add/city" class="btn btn-primary pull-right">
              <i class="fa fa-plus" aria-hidden="true"></i>
            </a>
          </th>
        </tr>
      </thead>
      <tbody>
        {{#each cities}}
        <tr>
          <th scope="row" class="align-middle">{{this.rowNumber}}</th>
          <td class="align-middle">{{this.city}}</td>
          <td class="align-middle">{{this.cityCode}}</td>
          <td class="align-middle">{{this.regionId}}</td>
          <td>
            <form action="/delete/city/{{this.cityCode}}" method="POST" class="d-inline pull-right">
              <button type="submit" class="btn btn-danger">
                <i class="fa fa-times" aria-hidden="true"></i>
              </button>
            </form>
          </td>
        </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</div>
