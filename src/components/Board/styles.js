import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  board: {
    flex: 1,
    justifyContent: 'space-between',
  },
  boardRow: {
    flex: 1,
    flexDirection: 'row',
  }
});

export { styles };
